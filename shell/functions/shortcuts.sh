# Shortcuts - Prefix (goto_...)

goto_home() {
  cd "$HOME" || exit
}

goto_files() {
  cd "$HOME/Files" || exit
}

goto_downloads() {
  cd "$HOME/Downloads" || exit
}

goto_programming() {
    cd "$HOME/Programming" || exit
}